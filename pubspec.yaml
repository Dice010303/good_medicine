name: good_medicine
description: "A new Flutter project for drug interaction check."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0
  http: ^1.2.0
  camera: ^0.11.0
  permission_handler: ^11.3.0
  google_mlkit_text_recognition: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true